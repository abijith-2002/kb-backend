# NOTE:
# Keep top-level libraries pinned, but allow transitive HTTP/async stack to resolve compatible versions.
# This avoids resolver conflicts and installation failures.

# Build helpers (improve wheels availability for some deps like chromadb/pandas)
setuptools>=68.0.0
wheel>=0.41.0

# Core runtime
fastapi==0.115.6
starlette==0.40.0
pydantic==2.10.4
typing_extensions>=4.8.0

# ASGI server and tooling
uvicorn==0.30.6
# Optional speedups (wheels available on linux; safe to keep)
uvloop==0.20.0
httptools==0.6.4
watchfiles==1.0.5
websockets==12.0

# Env and utilities
python-dotenv==1.0.1
PyYAML==6.0.2
packaging>=23.2

# Email validation
email_validator==2.2.0
dnspython==2.6.1

# Multipart form-data
python-multipart==0.0.12

# Supabase and Auth/JWT
supabase==2.10.0
python-jose[cryptography]==3.3.0
PyJWT==2.9.0

# Vector store and embeddings
chromadb==0.5.17
nomic==3.2.28  # provides nomic-embed-text via SDK

# LLM - Gemini
google-generativeai==0.8.3

# File parsing
python-docx==1.1.2
pdfminer.six==20240706
pandas==2.2.2
openpyxl==3.1.5

# Dev tooling (optional; keep pinned to avoid conflicts)
flake8==7.1.1
pytest==8.3.3
rich==13.9.4

# Optional FastAPI CLI for local dev
fastapi-cli==0.0.5
